{{ if eq .chezmoi.os "windows" }}
enabled_backends = ["winget", "cargo"]
{{ end }}

{{ if eq .chezmoi.os "linux" }}
enabled_backends = ["arch", "cargo"]
arch_package_manager = "yay"
{{ end }}

# For the random people who can't handle Neovim
vscode_variant = "code"

hostname_groups_enabled = true

[hostname_groups]
laptop-win = ["windows"]
